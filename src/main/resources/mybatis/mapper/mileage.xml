<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.bteam.proj.mileage.dao.IMileageDAO">

<update id="mileageUp" parameterType="map">
	UPDATE members
	SET
	    mem_mileage = mem_mileage + #{mileage}
	WHERE mem_no = #{memNo}
</update>

<update id="mileageUpByCpNo" parameterType="map">
	UPDATE members
	SET
	    mem_mileage = mem_mileage + #{mileage}
	WHERE mem_no = (
	SELECT mem_no
	FROM carpool
	WHERE cp_no = #{cpNo})
</update>

<update id="mileageDown" parameterType="map">
	UPDATE members
	SET
	    mem_mileage = mem_mileage - #{mileage}
	WHERE mem_no = #{memNo}
</update>


</mapper>